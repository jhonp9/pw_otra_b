// jhonp9/pw_otra_b/pw_otra_b-eb410fb86ee1e2f99aeca94d5e7031bc09cf3d3a/prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario {
  id              Int       @id @default(autoincrement())
  nombre          String
  email           String    @unique
  password        String
  rol             String    // 'streamer' o 'espectador'
  monedas         Int       @default(100) 
  puntosXP        Int       @default(0)
  nivelEspectador Int       @default(1)
  metaXp          Int       @default(1000)
  horasStream     Float     @default(0.0)
  nivelStreamer   Int       @default(1)
  
  streams         Stream[]
  regalosPropios  Regalo[]  
  transacciones   Transaccion[] @relation("Emisor")
  recibidos       Transaccion[] @relation("Receptor") // Relación inversa añadida
}

model Stream {
  id          Int       @id @default(autoincrement())
  titulo      String
  categoria   String
  estaEnVivo  Boolean   @default(false)
  inicio      DateTime?
  fin         DateTime?
  usuarioId   Int
  usuario     Usuario   @relation(fields: [usuarioId], references: [id])
}

model Regalo {
  id          Int     @id @default(autoincrement())
  nombre      String
  icono       String
  costo       Int
  puntos      Int
  streamerId  Int?
  streamer    Usuario? @relation(fields: [streamerId], references: [id])
}

model Transaccion {
  id              Int      @id @default(autoincrement())
  usuarioId       Int      // El que gasta/envía
  destinatarioId  Int?     // El que recibe (Nuevo campo opcional)
  monto           Int
  tipo            String   
  detalle         String?
  fecha           DateTime @default(now())
  
  usuario         Usuario  @relation("Emisor", fields: [usuarioId], references: [id])
  destinatario    Usuario? @relation("Receptor", fields: [destinatarioId], references: [id])
}
model Mensaje {
  id            Int      @id @default(autoincrement())
  contenido     String
  usuarioId     Int
  usuarioNombre String
  nivelUsuario  Int      // Guardamos el nivel que tenía al escribir
  rolUsuario    String
  streamId      Int      // ID del dueño del canal (Usuario ID del streamer)
  fecha         DateTime @default(now())
}